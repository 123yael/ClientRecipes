import { DarkLightDirective } from './dark-light.directive';

describe('DarkLightDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkLightDirective();
    expect(directive).toBeTruthy();
  });
});
